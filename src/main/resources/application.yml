# Spring Configuration
spring:
  profiles:
    active: dev,local
  datasource:
    url: "jdbc:mysql://localhost/stockdata"
    username: root
    password: pass
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: create-drop